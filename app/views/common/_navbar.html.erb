<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="/">NTPU MIS 後台</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class=" nav navbar-nav navbar-right">
       <% if !current_user %>
          <li> <%= link_to("登入", new_user_session_path) %> </li>
        <% else %>
          <li class="header_navbar"> <%= link_to("教師管理", teachers_path) %> </li>
          <li class="header_navbar"> <%= link_to("學生管理", students_path) %> </li>
          <li class="header_navbar"> <%= link_to("論文管理", theses_path) %> </li>
          <li class="header_navbar"> <%= link_to("公告管理", posts_path) %> </li>
          <li class="header_navbar"> <%= link_to("活動管理", events_path) %> </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Hi!, <%= current_user.name %>
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
               <li> <%= link_to("帳號設定", edit_user_registration_path )%></li>
              <li> <%= link_to("登出", destroy_user_session_path, method: :delete) %> </li>
            </ul>
          </li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
