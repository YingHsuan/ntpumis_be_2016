<div class="col-md-12">
<div class="group">
  <a href="./new"><button class="btn btn-primary pull-right">新增老師</button></a>
</div>
  <table class="table table-hover">
    <thead>
      <tr>
        <td>編號</td>
        <td>中文名字</td>
        <td>職位</td>
        <td>領域</td>
      </tr>
    </thead>
    <tbody>
      <% @teachers.each do |teacher| %>
        <tr>

          <td><%= link_to(teacher.id, teacher_path(teacher)) %></td>
          <td><%= teacher.name_c %></td>
          <td><%= teacher.title_c %></td>
          <td><%= teacher.domain_c %></td>
          <td>
           </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
